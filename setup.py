from setuptools import setup

setup(
   name='Scyscrapers',
   version='1.0',
   description='A module for checking if a board for the game "Skyscrapers" is winning or not',
   author='Krasniukk',
   author_email='tetiana.krasniuk@ucu.edu.ua',
   packages=['Skyscrapers'],  #same as name
   install_requires=''
)